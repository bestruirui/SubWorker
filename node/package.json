{
    "name": "subworker-node",
    "version": "1.0.0",
    "type": "module",
    "scripts": {
        "build:backend": "cd ../backend && pnpm install && pnpm build",
        "build": "node build.js",
        "start": "node dist/main.js",
        "dev": "node src/main.js"
    },
    "devDependencies": {
        "esbuild": "^0.27.0"
    }
}